{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.16.0/.schema/devbox.schema.json",
  "packages": [
    "atuin@latest",
    "nodejs@latest",
    "git@latest",
    "gitmoji-cli@latest"
  ],
  "shell": {
    "init_hook": [
      "if [ ! -d \"$HOME/.platformio/platforms/espressif32\" ]; then echo 'Installing ESP32 platform (one-time)…' && pio pkg install espressif32 --with-all-packages; fi"
    ]
  }
}
