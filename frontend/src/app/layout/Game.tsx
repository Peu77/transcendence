import { h } from "refreshjs";
import { GameModeSelector } from "./GameModeSelector";

export default function Game() {
    return (
        <div>
            <GameModeSelector />
        </div>
    );
}